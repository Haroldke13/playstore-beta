# Every environment variable this app reads, with the value to use on cPanel.
# Set these in cPanel > Setup Python App > Environment variables, NOT in a file
# inside the document root.

SITE_DOMAIN=playstore-beta.harold-datascience.co.ke
SITE_SCHEME=https
SITE_NAME=Playstore Beta

FLASK_DEBUG=0
TEMPLATES_AUTO_RELOAD=0
